# Next Technical Step

The next code change should be the first reversible extraction from `index.html`:

1. create `js/config.js`
2. define the `ARTIST` object for Renée
3. move only artist-specific values into that object
4. replace hard-coded identity/contact/asset/catalog values with configuration references
5. leave visual CSS and rendering behavior unchanged
6. verify the public experience is visually equivalent

Do not introduce a framework, bundler, database or CMS in this step.
