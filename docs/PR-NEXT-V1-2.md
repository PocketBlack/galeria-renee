# Next PR V1.2

The next change should be implementation-focused:

- load `js/config.js` from `index.html`
- migrate title/description/contact/assets/catalog references incrementally
- keep the current visual output unchanged
- remove duplicated Renée-specific literals only after each reference is verified
- validate the second-artist reuse test
